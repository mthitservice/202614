events{}
http {
    upstream backend {
        server backend:5000;
    }
    upstream frontend {
        server frontend:80;
    }

    server{
        listen 80;
        server_name mth-dell04;
        location / {
            proxy_pass http://frontend;
        }
        location /api {
            proxy_pass http://backend;
        }
    }

}